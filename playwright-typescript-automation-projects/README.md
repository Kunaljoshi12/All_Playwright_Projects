# Playwright XPath Axes Validation Framework

A TypeScript-based Playwright test project that validates all major XPath axes (self, parent, child, ancestor, descendant, following, following-sibling, preceding, preceding-sibling) against a live HTML table on W3Schools. The project demonstrates structured locator strategies using XPath axis navigation for table element selection and assertion.
